<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="apod_wallpaper.WinUI.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18" Spacing="6">
            <TextBlock FontSize="30" FontWeight="SemiBold" Text="Favorites" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="Saved local APOD images appear here."
                TextWrapping="Wrap" />
        </StackPanel>

        <InfoBar
            x:Name="FavoritesStatusBar"
            Grid.Row="1"
            IsClosable="False"
            IsOpen="True"
            Severity="Informational"
            Title="Loading favorite images"
            Visibility="Collapsed" />

        <Grid Grid.Row="2">
            <ListView
                x:Name="FavoritesListView"
                SelectionMode="None"
                Visibility="Collapsed" />

            <StackPanel
                x:Name="EmptyFavoritesPanel"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="360"
                Spacing="10">
                <FontIcon
                    HorizontalAlignment="Center"
                    Glyph="&#xE734;"
                    FontSize="38"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock
                    x:Name="EmptyFavoritesTitleText"
                    HorizontalAlignment="Center"
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="No favorite images yet" />
                <TextBlock
                    x:Name="EmptyFavoritesDescriptionText"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Add local APOD images to favorites from the Calendar preview."
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
