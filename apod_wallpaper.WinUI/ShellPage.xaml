<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="apod_wallpaper.WinUI.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:apod_wallpaper.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Padding="20"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="6">
                    <TextBlock FontSize="28" FontWeight="SemiBold" Text="APOD Wallpaper" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Windows desktop shell over the shared backend. This layout is the host container for preview, settings, and actions."
                        TextWrapping="Wrap" />
                </StackPanel>

                <Border
                    Grid.Column="1"
                    Padding="12,8"
                    Background="{ThemeResource AccentFillColorSecondaryBrush}"
                    CornerRadius="999">
                    <TextBlock x:Name="HostBadgeText" FontWeight="SemiBold" Text="Packaged WinUI Host" />
                </Border>
            </Grid>
        </Border>

        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" OverflowButtonVisibility="Collapsed">
            <AppBarButton
                x:Name="PreviewButton"
                Click="PreviewButton_Click"
                Icon="Preview"
                Label="Preview" />
            <AppBarButton
                x:Name="SettingsButton"
                Click="SettingsButton_Click"
                Icon="Setting"
                Label="Settings" />
            <AppBarButton
                x:Name="AboutButton"
                Click="AboutButton_Click"
                Icon="Help"
                Label="About" />
            <AppBarSeparator />
            <AppBarButton
                Click="HideToTrayButton_Click"
                Icon="HideBcc"
                Label="Hide to tray" />
        </CommandBar>

        <NavigationView
            x:Name="AppNavigationView"
            Grid.Row="2"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="True"
            IsSettingsVisible="False"
            PaneDisplayMode="LeftCompact"
            SelectionChanged="AppNavigationView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="PreviewNavItem" Content="Preview" Icon="Preview" Tag="preview" />
                <NavigationViewItem x:Name="SettingsNavItem" Content="Settings" Icon="Setting" Tag="settings" />
                <NavigationViewItem x:Name="AboutNavItem" Content="About" Icon="Help" Tag="about" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Page>
